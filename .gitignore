# Gradle
build/
.gradle/
bin/

# Build-Output (z. B. von Eclipse-Plugins / Vaadin-Frontend-Build erzeugt)
target/

# Eclipse
.classpath
.project
.settings/
.metadata/
RemoteSystemsTempFiles/

# Maschinenspezifischer TLS-Truststore (aus dem Windows-Zertifikatspeicher exportiert)
certs/

# Claude Code – persönliche Einstellungen (NIEMALS versionieren)
.claude/
CLAUDE.md

# Lokale statische Vorschau (kein Bestandteil des Projekts)
preview/

# Vaadin / Frontend (in beliebigen Modulen, daher mit **/ )
node_modules/
**/frontend/generated/
**/src/main/frontend/generated/
package-lock.json
pnpm-lock.yaml
vite.config.ts
vite.generated.ts
tsconfig.json
types.d.ts

# Vaadin Frontend-Development-Bundle: 2+ MB Binaerdatei, bei jedem Frontend-Build neu erzeugt.
# Nicht mergebar -> Konflikte bei Pull/Merge. Wird bei Bedarf lokal regeneriert.
**/src/main/bundles/dev.bundle

# Betriebssystem
Thumbs.db
.DS_Store
